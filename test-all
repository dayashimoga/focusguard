#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "${SCRIPT_DIR}"

if command -v flutter &> /dev/null; then
    flutter test --coverage "$@"
else
    podman exec -w /workspace focusguard-dev flutter test --coverage "$@"
fi
