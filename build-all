#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "${SCRIPT_DIR}"

if command -v dart &> /dev/null; then
    dart run tool/build_all.dart "$@"
else
    podman exec -w /workspace focusguard-dev dart run tool/build_all.dart "$@"
fi
